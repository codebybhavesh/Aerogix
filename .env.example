# Optional: URL of the standalone Socket.io signaling server (see /server/README.md).
# If unset, the app falls back to http://localhost:4000 (local dev only).
VITE_SOCKET_URL=https://your-socket-server.example.com
